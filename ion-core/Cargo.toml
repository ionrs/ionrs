[package]
name = "ion-core"
version = "0.1.0"
edition = "2021"

[features]
default = []
obfuscate = ["obfstr"]

[dependencies]
obfstr = { version = "0.4", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
indexmap = { version = "2", features = ["serde"] }
